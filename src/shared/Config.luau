return {
	Roles = { "Runner", "Navigator", "Tracker" },
	WalkSpeed = 14,
	Tick = 0.2,
	ScanInterval = 1,
	ResultDuration = 8,
	-- Supply only audio you own or have permission to use. Empty hooks are silent.
	Sounds = { Ambient = "", Monster = "" },
}
